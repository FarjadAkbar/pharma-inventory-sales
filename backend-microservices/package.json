{
  "name": "backend-microservices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "pnpm install",
    "seed": "pnpm --filter @repo/shared build && pnpm --filter seeder seed",
    "dev": "pnpm -r --parallel dev",
    "dev:gateway": "pnpm --filter api-gateway dev",
    "dev:identity": "pnpm --filter identity-service dev",
    "dev:master-data": "pnpm --filter master-data-service dev",
    "dev:quality": "pnpm --filter quality-service dev",
    "dev:procurement": "pnpm --filter procurement-service dev",
    "build": "pnpm -r build",
    "migration:generate": "pnpm --filter identity-service migration:generate",
    "migration:run": "pnpm --filter identity-service migration:run",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "ts-node": "^10.9.0",
    "prettier": "^3.1.0"
  }
}